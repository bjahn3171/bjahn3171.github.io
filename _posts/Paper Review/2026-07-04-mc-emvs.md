---
title: "[논문리뷰] Multi-Event-Camera Depth Estimation and Outlier Rejection by Refocused Events Fusion"
summary: "MC-EMVS 논문 리뷰 (AISY 2022)"
excerpt: "MC-EMVS 논문 리뷰 (AISY 2022)"
date: 2026-07-04
last_modified_at: 2026-07-04

categories:
  - 논문리뷰

tags:
  - 3D Vision
  - Event Camera
  - Depth Estimation
  - AISY

layout: single
author_profile: true
toc: true
toc_sticky: true
comments: false
mathjax: true

paper:
  title: "Multi-Event-Camera Depth Estimation and Outlier Rejection by Refocused Events Fusion"
  venue: "Advanced Intelligent Systems (AISY)"
  year: "2022"
  date: "23 Sep 2022"
  authors: "Suman Ghosh, Guillermo Gallego"
  affiliations: "Technische Universität Berlin"
  links:
    - name: "Paper"
      url: "https://arxiv.org/abs/1611.01704"
    - name: "Github"
      url: "https://github.com/tub-rip/dvs_mcemvs"
    - name: "Project"
      url: ""
---

{% include paper-info.html %}

## 한 줄 요약

<div class="paper-summary-box" markdown="1">

**TL;DR.**  
이 논문은 CNN 기반 비선형 변환, 양자화, 복원 변환을 하나의 rate–distortion 목적함수로 end-to-end 최적화하여 기존 JPEG/JPEG 2000보다 우수한 압축 성능을 얻는 학습 기반 이미지 압축 방법을 제안한다.

</div>

---

<figure class="paper-figure paper-figure--wide">
  <img src="{{ '/assets/images/paper_review/mc-evms/front.png' | relative_url }}" alt="MCEVMS overview">
</figure>

---

## Abstract / 요약

### 1. Original Abstract

<details markdown="1">
<summary><strong>Original Abstract 보기</strong></summary>

> Event cameras are bio-inspired sensors that offer advantages over traditional cameras. They operate asynchronously,
sampling the scene at microsecond resolution and producing a stream of brightness changes. This unconventional output has sparked
novel computer vision methods to unlock the camera’s potential. Here, the problem of event-based stereo 3D reconstruction for SLAM
is considered. Most event-based stereo methods attempt to exploit the high temporal resolution of the camera and the simultaneity of
events across cameras to establish matches and estimate depth. By contrast, this work investigates how to estimate depth without
explicit data association by fusing Disparity Space Images (DSIs) originated in efficient monocular methods. Fusion theory is
developed and applied to design multi-camera 3D reconstruction algorithms that produce state-of-the-art results, as confirmed by
comparisons with four baseline methods and tests on a variety of available datasets.
</details>

### 2. 한국어 요약

본 논문은 SLAM을 위한 이벤트 기반 스테레오 3차원 재구성 문제를 다룬다. 기존의 대부분의 이벤트 기반 스테레오 방법은 이벤트 카메라의 높은 시간 해상도와 다중 카메라에서 발생하는 이벤트 간 동시성을 활용하여 대응 관계를 찾고, 이를 바탕으로 깊이를 추정한다.
반면 본 연구는 명시적인 이벤트 매칭 과정 없이 깊이를 추정하는 방법을 제안한다. 이를 위해 단안 방식으로 생성된 Disparity Space Image(DSI)를 융합하여 스테레오 깊이 정보를 구성한다.
또한 본 논문은 DSI 융합에 대한 이론적 기반을 제시하고, 이를 다중 카메라 기반 3차원 재구성 알고리즘 설계에 적용한다. 제안 방법은 네 가지 baseline 방법과의 비교 및 다양한 공개 데이터셋에서의 실험을 통해 기존 방법 대비 우수한 성능을 보임을 확인하였다.

---

## 서론

본 논문은 VO/SLAM을 위한 이벤트 기반 스테레오 3차원 재구성 문제를 다룬다. 이벤트 카메라는 밝기 변화가 발생한 순간에만 이벤트를 비동기적으로 출력하는 뉴로모픽 센서로, 높은 시간 해상도와 넓은 동적 범위, 낮은 전력 소비라는 장점을 가진다. 
이러한 특성은 고속 움직임이나 극한 조명 환경에서 유용하지만, 이벤트 스트림을 처리하기 위한 별도의 알고리즘이 필요하다.

본 연구는 이벤트 기반 단안 VO 시스템인 EVO와 그 핵심 모듈인 EMVS에서 출발한다. EMVS는 이미지 복원이나 명시적인 이벤트 매칭 없이도 효율적인 3차원 재구성이 가능하다는 장점을 가진다.
논문은 이를 공통 clock을 공유하는 다중 이벤트 카메라, 특히 스테레오 설정으로 확장하고, 재초점화된 이벤트 정보를 융합하기 위한 이론을 제시한다. 이를 통해 단안 방식보다 더 정확한 깊이 추정과 이상치 제거가 가능함을 보인다.

---

## 배경

### 1. 이벤트 카메라 모델

Dynamic Vision Sensor(DVS)와 같은 이벤트 카메라는 일반적인 프레임 기반 카메라처럼 일정한 시간 간격으로 영상을 획득하지 않는다. 대신 각 픽셀에서 밝기 변화가 일정 임계값을 초과할 때마다 비동기적으로 이벤트를 발생시킨다.

하나의 이벤트는 다음과 같이 표현된다.

$$
e_k = (\mathbf{x}_k, t_k, p_k)
$$

여기서 $\mathbf{x}_k = (x_k, y_k)^\top$는 이벤트가 발생한 픽셀 위치, $t_k$는 이벤트 발생 시간, $p_k \in \{+1, -1\}$는 밝기 변화의 polarity를 의미한다.

이벤트는 같은 픽셀에서 이전 이벤트가 발생한 시점과 현재 시점 사이의 로그 밝기 변화가 contrast threshold $\theta$를 초과할 때 발생한다.

$$
L(\mathbf{x}_k, t_k) - L(\mathbf{x}_k, t_k - \Delta t_k) = p_k \theta
$$

즉, 로그 밝기가 충분히 증가하면 positive event가, 충분히 감소하면 negative event가 발생한다. 이처럼 이벤트 카메라는 모든 픽셀을 고정된 frame rate로 샘플링하지 않고, 각 픽셀이 독립적인 sampling rate를 갖는다.

조명이 일정하다고 가정하면 이벤트는 주로 장면의 움직임과 texture가 있는 위치에서 발생한다. 따라서 이벤트 스트림은 장면 전체가 아니라 변화가 있는 구조적 영역을 중심으로 생성된다. EMVS는 이러한 이벤트의 특성을 이용해 3차원 구조를 추정한다.

### 2. EMVS: 단안 3차원 재구성

EMVS(Event-based Multi-View Stereo)는 이벤트 카메라와 카메라 pose가 주어졌을 때, 명시적인 이벤트 matching 없이 장면의 3차원 구조를 추정하는 단안 기반 방법이다.

EMVS는 크게 두 단계로 구성된다. 먼저 이벤트를 3차원 공간으로 back-projection하여 Disparity Space Image(DSI)를 생성하고, 이후 DSI에서 local maximum을 검출하여 semi-dense depth map을 추정한다.

카메라가 움직이는 동안 이벤트는 서로 다른 시점에서 발생한다. 각 이벤트는 해당 시점의 카메라 pose를 이용해 3차원 공간상의 ray로 back-project될 수 있다. 실제 3차원 edge에 해당하는 위치에서는 여러 ray가 서로 교차하거나 밀집하므로, DSI에서 ray density가 높은 위치는 실제 장면 구조가 존재할 가능성이 높은 지점으로 해석된다.

DSI는 reference view를 기준으로 정의된 projective voxel grid 위에 구성된다. 이후 각 reference pixel에 대해 depth 방향으로 DSI 값을 탐색하고, 가장 큰 값을 갖는 위치를 해당 픽셀의 깊이로 선택한다.

이처럼 EMVS는 이벤트를 직접 서로 대응시키지 않고도 깊이를 추정할 수 있다. 본 논문은 이 장점을 스테레오 카메라 설정으로 확장한다.



---
## 4. Method

본 논문은 EMVS의 DSI 기반 깊이 추정 구조를 다중 이벤트 카메라 설정으로 확장한다. 입력은 동기화되어 있고 서로 강체로 고정된 두 개 이상의 이벤트 카메라에서 발생한 이벤트 스트림, 카메라 trajectory, 그리고 intrinsic/extrinsic calibration 정보이다.

기존의 단순한 접근은 각 카메라에 대해 EMVS를 독립적으로 수행한 뒤, 결과 point cloud를 후처리 단계에서 합치는 것이다. 그러나 이는 late fusion 방식으로, 두 카메라가 같은 장면을 동시에 관측한다는 장점을 충분히 활용하지 못한다.

반면 본 논문은 더 이른 단계인 DSI 단계에서 fusion을 수행한다. 즉, 각 카메라 이벤트로부터 별도의 DSI를 만들고, 이들을 하나의 fused DSI로 결합한 뒤, 그 fused DSI에서 depth map을 추출한다.

전체 과정은 다음과 같다.

1. 공통 reference view를 정의한다.
2. 각 이벤트 카메라의 이벤트를 3D 공간으로 back-project하여 카메라별 DSI를 생성한다.
3. 여러 DSI를 voxel-wise fusion function으로 융합한다.
4. fused DSI에서 각 reference pixel의 depth 방향 local maximum을 찾는다.
5. depth map과 confidence map을 생성하고, thresholding을 통해 semi-dense depth map을 얻는다.

---

### 1. 공통 Reference View와 카메라별 DSI 생성

스테레오 이벤트 카메라에서는 왼쪽 카메라 이벤트와 오른쪽 카메라 이벤트가 각각 존재한다. 본 논문은 두 카메라의 이벤트를 하나의 DSI에 바로 합산하지 않고, 먼저 카메라별 DSI를 따로 생성한다.

이를 위해 하나의 공통 reference view를 정의한다. 예를 들어 시간 구간 $[0, T]$의 중간 시점인 $t = T/2$에서의 왼쪽 카메라 pose를 reference view로 사용할 수 있다. 이렇게 하면 왼쪽 이벤트와 오른쪽 이벤트를 모두 동일한 3D voxel grid 위로 back-project할 수 있다.

왼쪽 카메라 이벤트로부터 생성된 DSI는 다음과 같이 해석할 수 있다.

$$
\mathrm{DSI}_L(X)
=
\text{왼쪽 카메라에서 온 ray들이 voxel } X \text{를 지나간 횟수}
$$

오른쪽 카메라에 대해서도 마찬가지로 다음과 같이 정의할 수 있다.

$$
\mathrm{DSI}_R(X)
=
\text{오른쪽 카메라에서 온 ray들이 voxel } X \text{를 지나간 횟수}
$$

여기서 중요한 점은 $\mathrm{DSI}_L$과 $\mathrm{DSI}_R$이 서로 다른 좌표계에 있는 것이 아니라, 동일한 reference view를 기준으로 정렬된 같은 3D 공간 위에 정의된다는 것이다. 따라서 두 DSI는 voxel 단위로 직접 비교하거나 융합할 수 있다.

이 방식은 서로 다른 reference view에서 생성된 DSI를 나중에 맞추는 경우 발생할 수 있는 resampling error를 줄일 수 있다.

---

### 2. 카메라 간 DSI Fusion

카메라별 DSI가 생성되면, 본 논문의 핵심 단계인 DSI fusion을 수행한다. 단순히 두 카메라의 이벤트 ray를 하나의 DSI에 모두 누적하는 방법도 가능하다. 이 경우 특정 voxel에 ray가 많이 모이면 3D 구조가 있다고 판단할 수 있다.

그러나 단순 합산은 ray가 어느 카메라에서 왔는지에 대한 정보를 잃어버린다. 예를 들어 어떤 voxel에 8개의 ray가 누적되었다고 할 때, 그 8개가 모두 한쪽 카메라에서만 온 것인지, 아니면 왼쪽과 오른쪽 카메라에서 각각 균형 있게 온 것인지는 중요한 정보이다.

실제 3D edge라면 두 카메라 모두에서 관측될 가능성이 높다. 반대로 한쪽 카메라에서만 강하게 나타나는 ray density는 occlusion, noise, 또는 잘못된 ray intersection일 수 있다. 따라서 본 논문은 두 DSI의 값을 단순히 더하지 않고, 두 카메라에서 모두 높은 ray density를 갖는 위치를 강조하는 fusion function을 사용한다.

가장 대표적으로 사용되는 함수는 harmonic mean이다.

$$
H(u, v)
=
\frac{2uv}{u + v}
$$

여기서 $u = \mathrm{DSI}_L(X)$, $v = \mathrm{DSI}_R(X)$이다.

Harmonic mean은 두 값이 모두 클 때만 큰 값을 갖는다. 한쪽 값만 크고 다른 쪽 값이 작으면 결과값도 작아진다. 따라서 이 함수는 다음과 같은 “AND” 성격을 가진다.

> $\mathrm{DSI}_L(X)$도 높고 $\mathrm{DSI}_R(X)$도 높아야 voxel $X$를 신뢰한다.

이 성질 때문에 harmonic mean은 한쪽 카메라에서만 발생한 spurious ray intersection을 억제하는 데 유리하다. 결과적으로 outlier가 줄어들고, 실제 3D 구조에 해당하는 위치가 fused DSI에서 더 뚜렷하게 나타난다.

---

### 3. Fusion Function의 의미

본 논문은 harmonic mean뿐 아니라 여러 fusion function을 비교한다. 대표적으로 arithmetic mean, geometric mean, harmonic mean, minimum, maximum, RMS 등이 사용된다.

이들은 다음과 같은 순서를 가진다.

$$
\min(u, v)
\le
H(u, v)
\le
G(u, v)
\le
A(u, v)
\le
\mathrm{RMS}(u, v)
\le
\max(u, v)
$$

여기서 arithmetic mean은 두 DSI 값을 평균내는 방식으로, 사실상 두 카메라의 ray count를 단순히 합산하는 것과 유사하다. 이 방식은 한쪽 카메라에서만 ray가 많이 누적되어도 높은 값을 만들 수 있기 때문에 outlier를 충분히 억제하지 못할 수 있다.

반면 harmonic mean이나 minimum은 두 카메라에서 모두 높은 응답이 나타나는 위치를 더 강하게 선택한다. 따라서 실제 3D edge에 해당하지 않는 우연한 ray 교차를 억제하는 효과가 있다.

다만 minimum은 너무 보수적이어서 한쪽 값을 기준으로 정보를 잘라버리는 경향이 있다. Harmonic mean은 두 입력을 모두 반영하면서도 한쪽만 큰 경우를 강하게 억제하므로, 본 논문에서는 카메라 간 DSI fusion에 적합한 함수로 다루어진다.

---

### 5. Depth Map과 Confidence Map 추출

DSI fusion이 끝나면 fused DSI에서 깊이를 추출한다. fused DSI를 $f(X)$라고 할 때, 각 reference pixel $(x, y)$에 대해 depth 방향으로 값을 탐색한다.

$$
Z^*(x, y)
=
\operatorname*{arg\,max}_{Z}
f(X(x, y, Z))
$$

즉, reference pixel $(x, y)$에서 가능한 depth 후보들 중 fused DSI 값이 가장 큰 위치를 해당 픽셀의 깊이로 선택한다.

동시에 해당 최대값은 confidence 값으로 사용된다.

$$
c^*(x, y)
=
\max_{Z}
f(X(x, y, Z))
$$

Confidence map은 해당 픽셀에서 얼마나 많은 ray가 일관되게 모였는지를 나타낸다. 따라서 confidence 값이 높은 픽셀은 실제 3D 구조일 가능성이 높다.

이후 Adaptive Gaussian Thresholding을 적용하여 confidence가 높은 픽셀만 선택하고, isolated point를 제거하기 위해 median filtering을 적용한다. 최종적으로 출력되는 결과는 장면의 edge 구조를 중심으로 한 semi-dense depth map이다.

---

## Experiments / Analysis

### 5.1 Experimental Setup

| 항목 | 내용 |
|---|---|
| Task | Lossy image compression |
| Baselines | JPEG, JPEG 2000 |
| Metric | Rate–distortion performance, MS-SSIM |
| Main comparison | bitrate 대비 복원 품질 |

### 5.2 Main Results

논문은 제안 방법이 JPEG 및 JPEG 2000보다 전반적으로 더 좋은 rate–distortion 성능을 보인다고 보고한다. 특히 낮은 bitrate에서 시각적 품질 향상이 두드러진다.

이는 학습 기반 변환이 사람이 설계한 고정 변환보다 이미지 통계에 더 잘 적응할 수 있음을 보여준다.

### 5.3 Additional Observations

이 논문에서 중요한 관찰은 단순한 픽셀 단위 오차뿐 아니라, MS-SSIM과 같은 perceptual quality 지표에서도 개선이 나타난다는 점이다. 즉, 제안 방법은 수치적 distortion뿐 아니라 사람이 보기에 더 자연스러운 복원을 목표로 한다.

---

## 6. Discussion

### 6.1 장점

1. 이미지 압축 전체 과정을 end-to-end로 최적화한다.
2. 양자화 문제를 연속적인 relaxation으로 다루어 학습 가능하게 만든다.
3. GDN 비선형성을 사용해 압축에 적합한 representation을 학습한다.
4. JPEG, JPEG 2000과 비교하여 우수한 rate–distortion 성능을 보인다.

### 6.2 한계 및 의문점

1. entropy model이 이후 neural compression 연구들에 비해 상대적으로 단순하다.
2. 실제 codec 시스템 관점에서는 entropy coding, runtime, model size까지 함께 고려할 필요가 있다.
3. 학습 데이터 분포와 다른 이미지에 대해 얼마나 일반화되는지는 추가적으로 볼 필요가 있다.

---

## 7. Takeaway

이 논문은 neural image compression의 초기 핵심 논문 중 하나로 볼 수 있다.  
가장 중요한 포인트는 압축 과정을 사람이 설계한 모듈의 조합이 아니라, **rate–distortion objective로 직접 학습되는 end-to-end optimization 문제**로 정식화했다는 점이다.

내가 가져갈 점은 다음과 같다.

- 손실 압축에서는 rate와 distortion을 동시에 고려해야 한다.
- 양자화는 미분 불가능하므로 학습을 위해 relaxation 또는 proxy가 필요하다.
- transform coding 구조는 neural network 기반으로도 자연스럽게 확장될 수 있다.
- 이후 hyperprior, autoregressive entropy model, VAE 기반 compression 연구의 출발점으로 이해할 수 있다.

---

## 8. 용어 정리

| 용어 | 의미 |
|---|---|
| Analysis transform | 입력 이미지를 latent representation으로 변환하는 인코더 |
| Synthesis transform | latent representation으로부터 이미지를 복원하는 디코더 |
| Uniform quantization | 연속값을 일정 간격의 이산값으로 변환하는 양자화 방식 |
| Rate–distortion optimization | bitrate와 복원 오차 사이의 trade-off를 최적화하는 문제 |
| GDN | Generalized Divisive Normalization, local gain control 형태의 비선형성 |
| MS-SSIM | 이미지 구조적 유사도를 평가하는 perceptual quality metric |

---

## 9. 최종 정리

이 논문은 이미지 압축을 end-to-end 학습 가능한 비선형 transform coding 문제로 정식화한 연구이다.  
분석 변환, 양자화, 합성 변환을 하나의 모델로 묶고, rate–distortion objective를 통해 직접 최적화한다는 점에서 이후 neural image compression 연구의 기반이 된다.
